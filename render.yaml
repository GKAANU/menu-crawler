services:
  - type: web
    name: menu-crawler
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    plan: starter
    healthCheckPath: /

